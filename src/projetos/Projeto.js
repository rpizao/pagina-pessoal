import React, { Component } from 'react';

export default class Projetos extends Component{
    render(){
        return (
            <div>
                Projetos (rascunhos) em andamento
            </div>
        );
    }
}